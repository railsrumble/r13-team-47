- work = present(work)
.media.work
  != work.thumb
  .content
    %div= work.name
    %div= link_to work.url, work.url
  - if show_delete
    = button_to "delete", [@team, work], method: 'delete', confirm: "You're deleting your hard work! Are you sure?"
