- @teams.each do |team|
  .team 
    %h2.name= link_to(team.name, team_path(team))
    .url= link_to(team.url, team_path(team))
    %h3.level= link_to('level '+ team.level.to_s, team_path(team))
    .score= link_to(team.score, team_path(team), class: 'green-button')